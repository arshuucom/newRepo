---
 - name: Testing yaml syntax
   host: Windows
   gather_facts: no
   tasks:
   - name: playbook for simple 1 GB partition
     hosts: localhost
     gather_facts: no
     connection: local
     tasks:
     - name: testing envirnoment
    
  